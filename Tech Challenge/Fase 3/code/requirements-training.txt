# Google Colab provides PyTorch and CUDA.
# Datasets 5 avoids the older fsspec cap that conflicted with Colab gcsfs.
# Transformers 5.17 uses Hub 1.x and requires PEFT >=0.19.1.
-r requirements-model.txt
datasets==5.0.1
bitsandbytes==0.50.2
# PEFT 0.20 rejects the older torchao bundled in some Colab runtimes.
torchao==0.17.0
